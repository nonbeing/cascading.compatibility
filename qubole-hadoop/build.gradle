 def hadoopVersion = distProps[ "distribution.version" ]


// get default hadoop configuration

apply from: "${rootDir}/settings/hadoop-settings.gradle"


dependencies {

      testRuntime fileTree(dir: "/usr/lib/hadoop", include: "*.jar")
      testRuntime fileTree(dir: "/usr/lib/hadoop/hadoopJars", include: "*.jar")
      testRuntime fileTree(dir: "/usr/lib/hadoop/lib", include: "*.jar").exclude("*junit*")
}

test {
	testLogging {
		exceptionFormat = 'full'
	}
}
